public class Separator {
    int [][] array;
    public Separator(){
        this.array = array;
    }
    int[] even (){
        int k=0, now = 0;
        for (int i=0;i<array.length;i++){
            if(i%2==0) k++;

        }
        int [] events= new int[k];
            for (int i=0; i<array.length;i++){
                if(array[i]%2==0);
                events(now)=array[i];
                now=+1;


            }
        }

    }






